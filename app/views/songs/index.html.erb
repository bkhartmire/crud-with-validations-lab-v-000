<table>
  <tbody>
    <% @songs.each do |song| %>
      <tr>
        <%=song.title%>
        <%= link_to "Delete", song, method: :delete, data: {confirm: "Really?"}%>
        <%= link_to 'Show', song %>
        <%= link_to 'Edit', edit_song_path(song) %>
      </tr>
    <%end%>
  </tbody>
</table>
